import java.io.BufferedReader;
import java.util.HashSet;
import java.util.regex.*;

public class Parser {
	static Matcher Parse(BufferedReader bf, HashSet<?> data, Pattern pattern) {
		//Matcher m = p.matcher(line);
		//m.matches();
		return null;
	}
}
